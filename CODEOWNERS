# 每个路径的默认审阅人。写在这里的人会被自动请求 review。
#
# ⚠️ 这个文件刻意放在仓库**根目录**：GitHub 认根目录 / `.github/` / `docs/`，
#    GitLab 认根目录 / `.gitlab/` / `docs/`——只有根目录是两边都认的位置。
#    放进 `.github/` 的话，GitLab 项目里它就是一个谁也不会读的文件。
#
# 语法两边基本一致：
#   GitHub  https://docs.github.com/repositories/managing-your-repositorys-settings-and-features/customizing-your-repository/about-code-owners
#   GitLab  https://docs.gitlab.com/user/project/codeowners/
#
# 配合分支保护才真正有约束力，否则只是「自动 @ 一下」：
#   GitHub  Settings → Branches → Require review from Code Owners
#   GitLab  Settings → Repository → Protected branches → Code owner approval

* @{{ repo-owner }}

# 越关键的路径越值得单独指定审阅人，例如：
# /deny.toml    @{{ repo-owner }}
# /Cargo.toml   @{{ repo-owner }}
