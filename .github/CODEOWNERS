# 每个路径的默认审阅人。写在这里的人会被自动请求 review。
# 语法见 https://docs.github.com/repositories/managing-your-repositorys-settings-and-features/customizing-your-repository/about-code-owners
#
# 配合分支保护规则（Settings → Branches → Require review from Code Owners）
# 才真正有约束力，否则只是「自动 @ 一下」。

* @{{ gh-username }}

# 越关键的路径越值得单独指定审阅人，例如：
# /.github/     @{{ gh-username }}
# /deny.toml    @{{ gh-username }}
